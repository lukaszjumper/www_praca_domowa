doctype html
head
  meta(charset='utf-8')
  meta(http-equiv='X-UA-Compatible' content='IE=edge')
  title Start quizu
  meta(name='viewport' content='width=device-width, initial-scale=1')
  link(rel='stylesheet' type='text/css' media='screen' href='/stylesheets/style.css')
.start_wrapper
  #stat_info
    h2.text_big
      | Witaj w quizie!
    h4.text_big
      | Najlepsze wyniki
    table#best
      thead
        tr
          th Lp.
          th Wynik
          th Gracz
      tbody#table_body
        tr#empty_row
          td -
          td -
          td -
  #game_conf
    h4.text_big
      | Wybierz quiz i kliknij przycisk aby zacz&aogon;&cacute;
    label(for='game_select') Quiz:
    select#game_select
      option arytmetyka
      option geometria
    a.undecorated
      #start.big_button
        | Zacznij!
  a(href="/logging/logout") Wyloguj się
script(src='/javascripts/start.js')
script= 'currentUser = `' + user + '`; let rawStats = `' + rawStats + '`; parseStats(rawStats); runStartScreen();'
